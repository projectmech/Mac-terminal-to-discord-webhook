# Mac-terminal-to-discord-webhook
Send text from your Mac Terminal directly to a Discord channel using a webhook. Paste the command, run it, and instantly deliver messages, logs, or command output to Discord — simple, fast, and no extra dependencies required.
